Rule of thumb:  
      Use   up -d   to start
      Use   down    to stop
      Use   exec    to enter
docker run = solo player.
docker compose = team manager (runs the whole team of containers that your app needs).

===========================================
docker compose up -d        // Create & Starts all containers (WordPress, MySQL, phpMyAdmin) multiple containers working together
docker compose run --rm wpcli wp plugin list    // Starts wp-cli , as it's not a service so it doesn't starts with the above command.

docker compose down         // Stop and remove containers, but keep volumes (DB data)
docker compose build        // Rebuild containers if you changed Dockerfile or dependencies
docker compose restart      // Restart all services

-------------------------------------------
docker pull image-name       // pull the image from docker hub
------
docker run image-name        // Create a container from an image & runs it
docker run -it image-name    // "    "  & open containers terminal
-----
docker start image-name      // Starts existing container
docker stop image-name       // Stop running container
exit                         //  "      "       "  that run with "-it"
-------------------------------------------

docker ps                   // List running containers
docker ps -a                // List all containers (including stopped ones)

docker logs <container_id>  // Show logs of a container
docker logs -f <container>  // Follow logs in real time (like tail -f)

docker exec -it <container> bash   // Open a shell inside the container
docker exec -it <container> sh     // Alternative shell if bash not available

docker images              // List all downloaded images
docker image rm <image_id> // Remove an image

docker volume ls           // List all volumes (your DB data lives here)
docker volume rm <volume>  // Remove a volume (wipes DB data)
  
---------------------------- Special for WordPress setup
docker compose up --build   // Force rebuild and restart (if you changed config)
docker exec -it wordpress bash   // Jump inside the WP container (replace "wordpress" with your service name)


docker network ls          // Show networks (Docker Compose makes one automatically)
