### Updating wp plugin is too easy
  1) first time you need to connect with svn by id, password after that you don't need to reconnect
  2) copy your updated plugin files -> go inside the folder that you commited via svn / OR / initialize svn into a new folder ->
  3) then pest your updated code inside "Trank" folder -> click replace -> back to the root folder -> right click -> click "SVN commit" ->
  4) write change loog -> Click OK
